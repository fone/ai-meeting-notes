# Production install: Turbo transcription, UI, and bundled cloud-provider clients.
# This deliberately excludes openai-whisper/Torch. Turbo uses faster-whisper
# and does not need a multi-gigabyte PyTorch/CUDA download. Legacy Whisper
# models are an explicit optional extra in pyproject.toml.
# Core
textual>=0.50.0
faster-whisper>=1.2.1
pyyaml>=6.0

# Cloud AI providers (optional in pyproject.toml, required here for the bundled setup scripts)
openai>=1.0.0
anthropic>=0.18.0
openrouter>=0.1.0
